# Create a file named `.env.local` (not committed) with:
#
#   VITE_MOCK_API=1
#   VITE_CHAT_ENABLED=1
#   VITE_ENABLE_LEARNING_LOOP=1
#   VITE_ACTIONS_ENABLED=1
#
# This makes the UI run with mock API responses (no backend required).
#
